usage: telegram-tools structure export [-h] --chat CHAT [--output OUTPUT]

options:
  -h, --help       show this help message and exit
  --chat CHAT      The chat: numeric ID, @username, link, or its tg:chat: rid
  --output OUTPUT  Where to write the blueprint JSON; without it, printed
